<%= form_with(model: book_absolution, local: true) do |form| %>
  <% if book_absolution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book_absolution.errors.count, "error") %> prohibited this book_absolution from being saved:</h2>

      <ul>
      <% book_absolution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
